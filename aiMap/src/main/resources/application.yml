spring:
  application:
    name: ai-map
  datasource:
    url: jdbc:mariadb://localhost:3306/taptalk?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: jang0504
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
  mail:
    host: smtp.naver.com
    port: 587
    username: jjoon1024@naver.com
    password: jang0504?
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.naver.com

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true
    instanceId: ${spring.application.name}:${server.port}

server:
  port: 8080

file:
  upload-dir: ./uploads
